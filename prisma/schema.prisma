datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

// modelos existentes en prisma/schema/*.prisma se cargan automáticamente por el esquema principal
